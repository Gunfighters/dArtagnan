import { logger } from '../logger.js';

/**
 * Express 라우트 설정
 */
export function setupRoutes(app) {
    // [REMOVED FOR PORTFOLIO - All internal API endpoints]

    logger.info('[API][System] HTTP 라우트 설정 완료');
}