FROM node:20-alpine
WORKDIR /app

COPY src/dArtagnan.Lobby/package.json ./
RUN npm install --production

COPY src/dArtagnan.Lobby/ ./

ENV PORT=3000 \
    GAME_IMAGE=dartagnan-server:latest \
    GAME_INTERNAL_PORT=7777

EXPOSE 3000

CMD ["node", "server.js"]


